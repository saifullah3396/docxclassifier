# @package args.data_args

defaults:
  - default

dataset_name: RvlCdip
dataset_config_name: default
dataset_dir: ${oc.env:DATA_ROOT_DIR}/documents/rvlcdip
dataset_cache_dir: ${args.data_args.dataset_dir}/huggingface/